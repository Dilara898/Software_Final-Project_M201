# `ai/` is supplied by the course and must not be modified (an edit there is
# an automatic deduction), so its pre-existing type errors are not ours to
# fix and are not reported. Everything we wrote is checked normally.
[mypy]
ignore_missing_imports = True

[mypy-ai.*]
ignore_errors = True
